// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: sample.proto

package grpcconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	http "net/http"
	grpc "shwgrpc/pkg/grpc"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// HelloServiceName is the fully-qualified name of the HelloService service.
	HelloServiceName = "shw.HelloService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// HelloServiceHelloProcedure is the fully-qualified name of the HelloService's Hello RPC.
	HelloServiceHelloProcedure = "/shw.HelloService/Hello"
)

// These variables are the protoreflect.Descriptor objects for the RPCs defined in this package.
var (
	helloServiceServiceDescriptor     = grpc.File_sample_proto.Services().ByName("HelloService")
	helloServiceHelloMethodDescriptor = helloServiceServiceDescriptor.Methods().ByName("Hello")
)

// HelloServiceClient is a client for the shw.HelloService service.
type HelloServiceClient interface {
	Hello(context.Context, *connect.Request[grpc.HelloRequest]) (*connect.Response[grpc.HelloResponse], error)
}

// NewHelloServiceClient constructs a client for the shw.HelloService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewHelloServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) HelloServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	return &helloServiceClient{
		hello: connect.NewClient[grpc.HelloRequest, grpc.HelloResponse](
			httpClient,
			baseURL+HelloServiceHelloProcedure,
			connect.WithSchema(helloServiceHelloMethodDescriptor),
			connect.WithClientOptions(opts...),
		),
	}
}

// helloServiceClient implements HelloServiceClient.
type helloServiceClient struct {
	hello *connect.Client[grpc.HelloRequest, grpc.HelloResponse]
}

// Hello calls shw.HelloService.Hello.
func (c *helloServiceClient) Hello(ctx context.Context, req *connect.Request[grpc.HelloRequest]) (*connect.Response[grpc.HelloResponse], error) {
	return c.hello.CallUnary(ctx, req)
}

// HelloServiceHandler is an implementation of the shw.HelloService service.
type HelloServiceHandler interface {
	Hello(context.Context, *connect.Request[grpc.HelloRequest]) (*connect.Response[grpc.HelloResponse], error)
}

// NewHelloServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewHelloServiceHandler(svc HelloServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	helloServiceHelloHandler := connect.NewUnaryHandler(
		HelloServiceHelloProcedure,
		svc.Hello,
		connect.WithSchema(helloServiceHelloMethodDescriptor),
		connect.WithHandlerOptions(opts...),
	)
	return "/shw.HelloService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case HelloServiceHelloProcedure:
			helloServiceHelloHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedHelloServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedHelloServiceHandler struct{}

func (UnimplementedHelloServiceHandler) Hello(context.Context, *connect.Request[grpc.HelloRequest]) (*connect.Response[grpc.HelloResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("shw.HelloService.Hello is not implemented"))
}
